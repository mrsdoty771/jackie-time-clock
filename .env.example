PORT=3000
NODE_ENV=development

# Required
DATABASE_URL=mongodb+srv://USER:PASSWORD@HOST/DATABASE?tls=true&retryWrites=true&w=majority
SESSION_SECRET=change-me-to-a-long-random-string

# Optional: seed a default company + manager on first run
DEFAULT_COMPANY_ID=MVC
DEFAULT_COMPANY_NAME=MVC
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123

# Optional: seed a landlord/super-admin user (run: node seed.js)
SUPER_ADMIN_COMPANY_ID=master-branch
SUPER_ADMIN_USERNAME=JackieAdmin
SUPER_ADMIN_EMAIL=you@yourdomain.com
SUPER_ADMIN_PASSWORD=change-me

# Optional: allow setting employee passwords manually via Manager UI
# (Managers can always reset passwords from the Edit Employee modal)

